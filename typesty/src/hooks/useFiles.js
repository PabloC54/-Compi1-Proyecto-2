import {useState} from 'react'
import { confirmAlert } from 'react-confirm-alert'

const useFiles = ( ) =>{
  //const [logs, setLogs] = useState([initialState])

  //const { tabs, activeTab, setContent, clearTab, newTab, changeTab, closeTab } = useTabs()
  

  //const handleFileOpen = () => inputFile.current.click()

  //return {tabs, activeTab, newFile}
}

export default useFiles
